# Core Flask Framework
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.6.3

# Database Support
psycopg2-binary==2.9.9   # PostgreSQL adapter (stable with Python 3.11)
SQLAlchemy==2.0.30       # ORM for database abstraction (stable with Python 3.11)
alembic==1.13.1          # Database migrations (compatible with SQLAlchemy 2.0)

# Environment & Configuration
python-dotenv==1.0.1    # Environment variable management
gunicorn==21.2.0        # Production WSGI server

# Security & Validation
Flask-Limiter==3.5.0    # Rate limiting
marshmallow==3.20.1     # Data validation and serialization

# Monitoring & Logging
structlog==24.1.0       # Structured logging
sentry-sdk[flask]==1.40.4  # Error tracking (optional)

# HTTP Requests
requests==2.32.4        # HTTP library for API calls

# Development & Testing
pytest==7.4.2           # Testing framework
pytest-flask==1.2.0     # Flask testing utilities
